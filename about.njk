---
layout: base.njk
title: About Reach For Youth
description: For over 50 years, Reach For Youth has been a trusted resource for youth and families in Central Indiana, providing mental health services, restorative justice programs, and prevention education.
---

<!-- HERO SECTION -->
<section class="hero hero--inner" aria-labelledby="hero-title">
  <div class="container">
    <div class="hero__content">
      <h1 id="hero-title" class="hero__title">{{ aboutContent.hero.title }}</h1>
      <p class="hero__subtitle">
        {{ aboutContent.hero.subtitle }}
      </p>
    </div>
  </div>
</section>

<!-- WHO WE ARE SECTION -->
<section class="section about-who-we-are" aria-labelledby="who-we-are-title">
  <div class="container">
    <div class="about-two-col">
      <div class="about-two-col__image">
        <img src="{{ aboutContent.whoWeAre.image or '/images/staff/2025 Summer Group.jpg' }}" alt="Reach For Youth team group photo" class="about-photo">
      </div>
      <div class="about-two-col__text">
        <h2 id="who-we-are-title" class="section-title" style="text-align:left;">Who We Are</h2>
        {% for paragraph in aboutContent.whoWeAre.paragraphs %}
        <p>{{ paragraph }}</p>
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<!-- WHAT WE BELIEVE SECTION -->
<section class="section about-believe" aria-labelledby="believe-title">
  <div class="container">
    <div class="about-two-col about-two-col--reverse">
      <div class="about-two-col__text">
        <h2 id="believe-title" class="section-title" style="text-align:left;color:#fff;">What We Believe</h2>
        <p>{{ aboutContent.whatWeBelieve.intro }}</p>
        <ul class="about-believe__list">
          {% for point in aboutContent.whatWeBelieve.points %}
          <li>{{ point }}</li>
          {% endfor %}
        </ul>
        <p>{{ aboutContent.whatWeBelieve.closing }}</p>
      </div>
      <div class="about-two-col__image">
        <img src="/images/staff/what-we-believe.jpg?v=2" alt="Reach For Youth staff members" class="about-photo">
      </div>
    </div>
  </div>
</section>

<!-- OUR IMPACT SECTION -->
<section class="section about-impact" aria-labelledby="impact-title">
  <div class="container">
    <div class="about-two-col">
      <div class="about-two-col__image">
        <img src="{{ aboutContent.ourImpact.image or '/images/staff/our-impact.jpg?v=2' }}" alt="Reach For Youth youth group" class="about-photo">
      </div>
      <div class="about-two-col__text">
        <h2 id="impact-title" class="section-title" style="text-align:left;">Our Impact</h2>
        <ul class="about-impact__list">
          {% for stat in aboutContent.ourImpact.stats %}
          <li>{{ stat.number }} {{ stat.label }}</li>
          {% endfor %}
          <li>{{ aboutContent.ourImpact.narrative }}</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<!-- OUR TEAM INTRO -->
<section class="section" aria-labelledby="team-title">
  <div class="container">
    <h2 id="team-title" class="section-title">{{ aboutContent.teamIntro.title }}</h2>
    <p class="section-subtitle" style="max-width:900px;margin:0 auto;text-align:center;">
      {{ aboutContent.teamIntro.text }}
    </p>
  </div>
</section>

<!-- STAFF DEPARTMENTS -->
{% for dept in staff.departments %}
<section class="section about-department" aria-labelledby="dept-{{ loop.index }}">
  <div class="container">
    <div class="department-banner">
      <h3 id="dept-{{ loop.index }}" class="department-banner__title">{{ dept.name }}</h3>
    </div>

    <div class="staff-bio-grid">
      {% for member in dept.members %}
      <article class="staff-bio-card">
        <div class="staff-bio-card__photo-wrap">
          {% if member.image %}
          <img src="{{ member.image }}" alt="{{ member.name }}" class="staff-bio-card__photo">
          {% else %}
          <div class="staff-bio-card__avatar">
            <svg aria-hidden="true"><use href="#icon-user"/></svg>
          </div>
          {% endif %}
        </div>
        <h4 class="staff-bio-card__name">{{ member.name }}</h4>
        <p class="staff-bio-card__title">{{ member.title }}</p>
        {% if member.bio %}
        <div class="staff-bio-card__bio">
          {{ member.bio | replace("\n\n", "</p><p>") | replace("\n", "<br>") | safe }}
        </div>
        {% endif %}
        {% if member.quote %}
        <blockquote class="staff-bio-card__quote">
          <p>"{{ member.quote }}"</p>
          {% if member.quoteAuthor %}
          <cite>- {{ member.quoteAuthor }}</cite>
          {% endif %}
        </blockquote>
        {% endif %}
      </article>
      {% endfor %}
    </div>
  </div>
</section>
{% endfor %}

<!-- BOARD OF DIRECTORS -->
<section class="section section--warm" aria-labelledby="board-title">
  <div class="container">
    <h2 id="board-title" class="section-title">Board of Directors</h2>

    <div class="board-tags">
      {% for officer in board.officers %}
      <div class="board-tag board-tag--officer">
        <span class="board-tag__name">{{ officer.name }}</span>
        {% if officer.role %}
        <span class="board-tag__role">{{ officer.role }}</span>
        {% endif %}
      </div>
      {% endfor %}

      {% for name in board.members %}
      <div class="board-tag">
        <span class="board-tag__name">{{ name }}</span>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<!-- HISTORY SECTION -->
<section class="section" aria-labelledby="history-title">
  <div class="container">
    <h2 id="history-title" class="section-title">50+ Years of <span>Service</span></h2>
    <p class="section-subtitle">
      Our journey from advocacy to comprehensive youth services
    </p>

    <div class="timeline">
      {% for item in timeline.items %}
      <div class="timeline__item">
        <div class="timeline__marker"></div>
        <div class="timeline__year">{{ item.year }}</div>
        <div class="timeline__content">
          {{ item.content }}
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<!-- PARTNERS SECTION -->
<section class="section section--warm" aria-labelledby="partners-title">
  <div class="container">
    <h2 id="partners-title" class="section-title">Our Funders & Partners</h2>
    <p class="partners__subtitle">
      We are grateful for the organizations that make our work possible.
    </p>

    <div class="partners__categories">
      <div class="partners__category">
        <h3 class="partners__category-title">{{ partners.foundation.title }}</h3>
        <div class="partners__grid">
          {% for partner in partners.foundation.items %}
          <div class="partner-item">
            <div class="partner-name">{{ partner }}</div>
          </div>
          {% endfor %}
        </div>
      </div>

      <div class="partners__category">
        <h3 class="partners__category-title">{{ partners.government.title }}</h3>
        <div class="partners__grid">
          {% for partner in partners.government.items %}
          <div class="partner-item">
            <div class="partner-name">{{ partner }}</div>
          </div>
          {% endfor %}
        </div>
      </div>

      <div class="partners__category">
        <h3 class="partners__category-title">{{ partners.community.title }}</h3>
        <div class="partners__grid">
          {% for partner in partners.community.items %}
          <div class="partner-item">
            <div class="partner-name">{{ partner }}</div>
          </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CTA SECTION -->
<section class="section cta-section" aria-labelledby="cta-title">
  <div class="container">
    <h2 id="cta-title" class="cta-section__title">Join Our Mission</h2>
    <p class="cta-section__text">
      Whether you volunteer your time, make a donation, or spread the word about our work, you can help transform lives and strengthen our community.
    </p>
    <div class="cta-section__buttons">
      <a href="/volunteer/" class="btn btn--secondary">Volunteer</a>
      <a href="https://give.reachforyouth.org" class="btn btn--primary">Donate</a>
    </div>
  </div>
</section>
